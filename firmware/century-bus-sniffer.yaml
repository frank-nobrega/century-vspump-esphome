substitutions:
  device_name: century-bus-sniffer
  friendly_name: Century Bus Sniffer

esphome:
  name: ${device_name}
  friendly_name: ${friendly_name}
  min_version: 2026.8.0

esp32:
  board: esp32-s3-devkitc-1
  framework:
    type: esp-idf

logger:
  # Keep the pump UART dedicated to capture. View logs through Wi-Fi/API.
  baud_rate: 0
  level: DEBUG
  tx_buffer_size: 2048

api:

ota:
  - platform: esphome

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

uart:
  id: sniff_uart
  # Receive only: there is deliberately no tx_pin, Modbus component, or pump
  # component in this temporary image.
  rx_pin: GPIO1
  # The known-working Hayward controller uses its proprietary pump protocol
  # at 19200 baud. A 9600-baud capture aliases the transmitted bit stream.
  baud_rate: 19200
  data_bits: 8
  parity: NONE
  stop_bits: 1
  rx_buffer_size: 1024
  debug:
    direction: RX
    dummy_receiver: true
    debug_prefix: "[PUMP BUS] "
    after:
      # At 19200 baud one 8-N-1 character takes about 0.52 ms. This separates
      # Hayward packets without splitting normally spaced bytes.
      timeout: 5ms
      bytes: 64
    sequence:
      - lambda: UARTDebug::log_hex(direction, bytes, ' ', debug_prefix);
